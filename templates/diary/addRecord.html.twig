{% extends 'base.html.twig' %}

{% block heading %}{% endblock %}

{% block body %}
    <h1>{{ 'Add a meal' | trans }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-success btn-lg pull-right">{{ 'Add' | trans }}</button>
    {{ form_end(form) }}
{% endblock %}